import React from 'react';
import { FcSynchronize } from "react-icons/fc";

const Loading = () => {
    return (
        <div className="loading">
            <span><FcSynchronize /></span>
        </div>
    );
};

export default Loading;